name: IPQ60XX-DualBuild

on:
  workflow_run:
    workflows: ["Auto-Clean"]
    types:
      - completed
  workflow_dispatch:

env:
  TZ: Asia/Shanghai

jobs:
  Build:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        repo:
          - name: LibWrt
            url: https://github.com/laipeng668/openwrt-6.x.git
            branch: k6.12-nss
            tag: IPQ60XX-LibWrt
          - name: ImmortalWrt
            url: https://github.com/VIKINGYFY/immortalwrt.git
            branch: main
            tag: IPQ60XX-ImmortalWrt

    env:
      CONFIG_FILE: configs/IPQ60XX.config
      GENERAL_CONFIG_FILE: configs/General.config
      DIY_SCRIPT: scripts/Roc-script.sh
      CLASH_KERNEL: amd64
      UPLOAD_BIN_DIR: false
      FIRMWARE_RELEASE: true

    steps:
      - name: Check Server Performance(检查服务器性能)
        run: |
          echo "警告⚠"
          echo "分配的服务器性能有限，若选择的插件过多，务必注意CPU性能！"
          echo -e "已知CPU型号（降序）：7763，8370C，8272CL，8171M，E5-2673 \n"
          echo "--------------------------CPU信息--------------------------"
          echo "CPU物理数量：$(cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l)"
          echo -e "CPU核心信息：$(cat /proc/cpuinfo | grep name | cut -f2 -d: | uniq -c) \n"
          echo "--------------------------内存信息--------------------------"
          echo "已安装内存详细信息："
          echo -e "$(sudo lshw -short -C memory | grep GiB) \n"
          echo "--------------------------硬盘信息--------------------------"
          echo "硬盘数量：$(ls /dev/sd* | grep -v [1-9] | wc -l)" && df -hT

      - name: Initialization Environment(初始化环境)
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -y update
          sudo -E apt-get -y install $(curl -fsSL is.gd/depends_ubuntu_2204)
          sudo -E systemctl daemon-reload
          sudo timedatectl set-timezone "$TZ"

      - name: Checkout Workflow(检出工作流代码)
        uses: actions/checkout@main

      - name: Clone Source Code(克隆源代码)
        run: |
          df -hT $GITHUB_WORKSPACE
          WORKSPACE_NAME="openwrt-${{ matrix.repo.name }}"
          sudo mkdir -p /mnt/$WORKSPACE_NAME
          sudo chown -R $(id -u):$(id -g) /mnt/$WORKSPACE_NAME
          git clone --depth 1 -b "${{ matrix.repo.branch }}" --single-branch "${{ matrix.repo.url }}" /mnt/$WORKSPACE_NAME
          cd /mnt/$WORKSPACE_NAME
          echo "OPENWRT_PATH=$PWD" >> $GITHUB_ENV
          VERSION_INFO=$(git show -s --date=short --format="作者: %an<br/>时间: %cd<br/>内容: %s<br/>hash: %H")
          echo "VERSION_INFO=$VERSION_INFO" >> $GITHUB_ENV
          # 兼容不同源码的kernel版本检测
          if [ -f target/linux/generic/kernel-6.12 ]; then
            VERSION_KERNEL=$(grep -oP 'LINUX_KERNEL_HASH-\K[0-9]+\.[0-9]+\.[0-9]+' target/linux/generic/kernel-6.12)
          elif [ -f include/kernel-version.mk ]; then
            VERSION_KERNEL=$(grep -oP 'LINUX_VERSION-\K[0-9]+\.[0-9]+\.[0-9]+' include/kernel-version.mk)
          else
            VERSION_KERNEL="Unknown"
          fi
          echo "VERSION_KERNEL=$VERSION_KERNEL" >> $GITHUB_ENV

      - name: Generate Variables(生成变量)
        run: |
          cp $CONFIG_FILE $OPENWRT_PATH/.config
          cd $OPENWRT_PATH
          make defconfig > /dev/null 2>&1
          SOURCE_REPO="$(basename -s .git ${{ matrix.repo.url }})"
          echo "SOURCE_REPO=$SOURCE_REPO" >> $GITHUB_ENV
          DEVICE_TARGET=$(grep CONFIG_TARGET_BOARD .config | awk -F '"' '{print $2}')
          DEVICE_SUBTARGET=$(grep CONFIG_TARGET_SUBTARGET .config | awk -F '"' '{print $2}')
          echo "DEVICE_TARGET=$DEVICE_TARGET" >> $GITHUB_ENV
          echo "DEVICE_SUBTARGET=$DEVICE_SUBTARGET" >> $GITHUB_ENV
          echo "HASH=$(git log -1 --pretty=format:'%h')" >> $GITHUB_ENV
          echo "CACHE_DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV

      - name: Cache Toolchain(缓存工具链)
        uses: actions/cache@main
        with:
          key: ${{ env.SOURCE_REPO }}-${{ matrix.repo.branch }}-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-${{ env.HASH }}-${{ env.CACHE_DATE }}
          restore-keys: ${{ env.SOURCE_REPO }}-${{ matrix.repo.branch }}-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}-
          path: |
            ${{ env.OPENWRT_PATH }}/.ccache
            ${{ env.OPENWRT_PATH }}/staging_dir

      - name: Refresh The Cache(刷新缓存)
        run: |
          if [ -d "$OPENWRT_PATH/staging_dir" ]; then
            find "$OPENWRT_PATH/staging_dir" -type d -name "stamp" -not -path "*target*" | while read -r dir; do
                find "$dir" -type f -exec touch {} +
            done
          fi

      - name: Install Feeds(安装feeds)
        run: |
          cd $OPENWRT_PATH
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Load Custom Configuration(加载自定义配置)
        run: |
          chmod +x $DIY_SCRIPT
          cd $OPENWRT_PATH
          $GITHUB_WORKSPACE/$DIY_SCRIPT

      - name: Download DL Package(下载DL软件包)
        run: |
          cat $CONFIG_FILE $GENERAL_CONFIG_FILE > $OPENWRT_PATH/.config
          cd $OPENWRT_PATH
          make defconfig
          make download -j$(nproc)

      - name: Compile Firmware(开始编译固件)
        id: compile
        run: |
          cd $OPENWRT_PATH
          echo -e "$(nproc) thread compile"
          make -j$(nproc) || make -j1 || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT
          echo "DATE=$(date +"%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV
          echo "FILE_DATE=$(date +"%Y.%m.%d")" >> $GITHUB_ENV

      - name: Check Space Usage(检查空间使用情况)
        if: (!cancelled())
        run: df -hT

      - name: Organize Files(整理文件 - 只保留固件)
        if: steps.compile.outputs.status == 'success'
        run: |
          cd $OPENWRT_PATH/bin/targets/*/*
          # 创建发布目录
          mkdir -p publish
          
          # 复制 factory.bin 文件
          find . -maxdepth 1 -type f -name "*factory.bin" -exec cp {} publish/ \;
          
          # 复制 sysupgrade.bin 文件
          find . -maxdepth 1 -type f -name "*sysupgrade.bin" -exec cp {} publish/ \;
          
          # 复制配置文件
          cp $OPENWRT_PATH/.config publish/ipq60xx.config
          
          # 生成 SHA256 校验文件
          (cd publish && sha256sum * > SHA256SUMS)
          
          echo "FIRMWARE_PATH=$PWD/publish" >> $GITHUB_ENV
          echo "✅ 当前发布目录文件列表："
          ls -la publish


      - name: Upload Firmware To Release(发布固件)
        if: steps.compile.outputs.status == 'success' && env.FIRMWARE_RELEASE == 'true'
        uses: ncipollo/release-action@main
        with:
          name: ${{ env.DATE }} for ${{ matrix.repo.tag }}
          allowUpdates: true
          tag: ${{ matrix.repo.tag }}
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: ${{ env.FIRMWARE_PATH }}/*
          body: |
            **This is OpenWrt Firmware for ${{ matrix.repo.tag }}**
            ### 📒 固件信息
            - 这是开启全功能NSS的6.12内核固件，默认主题为Argon；该固件在 ${{ matrix.repo.name }} 的基础上，添加了额外的软件包，并添加了若干工具，具体详见IPQ60XX.config。
            - 💻 这是 ${{ matrix.repo.tag }} 平台使用的 OpenWrt 固件
            - ⚽ 固件源码: ${{ matrix.repo.url }}
            - 💝 源码分支: ${{ matrix.repo.branch }}
            - 🌐 默认地址: **192.168.2.1**
            - 🔑 默认密码: none
            ### 🧊 固件版本
            - 固件内核版本：**${{ env.VERSION_KERNEL }}**
            - 固件编译前最后一次➦[主源码](${{ matrix.repo.url }})更新记录
            - ${{ env.VERSION_INFO }}

      - name: Delete Old Cache(删除旧缓存)
        if: always()
        run: |
          # 获取缓存列表并删除
          gh cache list --key ${{ env.SOURCE_REPO }}-${{ matrix.repo.branch }}-${{ env.DEVICE_TARGET }}-${{ env.DEVICE_SUBTARGET }}- --json key --jq '.[] | .key' | while read -r key; do
            gh cache delete "$key" || true
          done
          # 输出缓存状态
          echo "========cache status========"
          echo "ccache: $(du -sh $OPENWRT_PATH/.ccache 2>/dev/null | cut -f 1 || echo 'N/A')"
          echo "staging: $(du -sh $OPENWRT_PATH/staging_dir 2>/dev/null | cut -f 1 || echo 'N/A')"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
